{
  "name": "dayflow-hrms",
  "version": "1.0.0",
  "private": true,
  "description": "Dayflow HRMS - Enterprise Workforce Operating System",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate --schema=./prisma/schema.postgres.prisma && npx prisma db push --schema=./prisma/schema.postgres.prisma --accept-data-loss && npx tsx prisma/seed.ts && cd ../frontend && npm install && npm run build",
    "postinstall": "cd backend && npx prisma generate --schema=./prisma/schema.postgres.prisma",
    "start": "cd backend && node dist/server.js",
    "test": "cd backend && npm test"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cookie-parser": "^1.4.7",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.8.7",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
